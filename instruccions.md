# Ordres principals de Git

- `git init` – Inicialitza un repositori nou
- `git status` – Mostra els canvis pendents
- `git add <fitxer>` – Afegeix fitxers a l'índex
- `git commit -m "missatge"` – Desa els canvis amb un missatge
- `git push origin <branca>` – Pujar els canvis al repositori remot
